<div style="width:250px; margin:0px auto 30px auto; padding-top:150px;">
<h1>Reset Password</h1>

<%= form_for @recruiter, :url => password_recruiterreset_path(params[:id]) do |f| %>
  <% if @recruiter.errors.any? %>
    <div class="error_messages">
      <h2>Form is invalid</h2>
      <ul>
        <% for message in @re
        .errors.full_messages %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field" style="padding:10px;">
    <%= f.label :password %>
    <%= f.password_field :password %>
  </div>
  <div class="field" style="padding:10px;">
    <%= f.label :password_confirmation %>
    <%= f.password_field :password_confirmation %>
  </div>
   <div class="actions" style="padding:10px; margin-bottom:300px;"><%= f.submit "Update Password" %></div>
  
<% end %>
</div>